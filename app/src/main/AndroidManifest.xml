<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.tuapp.pizzas">

    <!-- Permiso de internet necesario para Firebase -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Eva3"
        tools:targetApi="31">

        <!-- MainActivity como la actividad de inicio -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- LoginActivity para autenticación -->
        <activity
            android:name=".LoginActivity"
            android:exported="true" />

        <!-- RegisterActivity para registro de nuevos usuarios -->
        <activity
            android:name=".RegisterActivity"
            android:exported="true" />

        <!-- AddEditPizzaActivity para agregar o editar pizzas -->
        <activity
            android:name=".AddEditPizzaActivity"
            android:exported="true" />

    </application>

</manifest>
